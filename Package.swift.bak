// swift-tools-version:5.5
import PackageDescription

let package = Package(
    name: "VoiceDictate",
    platforms: [.macOS(.v12)],
    dependencies: [],
    targets: [
        .executableTarget(
            name: "VoiceDictate",
            dependencies: [],
            path: "src/VoiceDictate/Sources"
        )
    ]
) 